{{#each books as |book|}}
  <section class='bookList' data-id="{{book.id}}">
    <h3 class='title'>
      {{book.title}}
    </h3>
    <h6>Author:
      {{#if book.author}}
        {{book.author}}
      {{else}}
        No author provided.
      {{/if}}
    </h6>
  </section>
{{/each}}
