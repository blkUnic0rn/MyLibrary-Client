{{#each books as |book|}}
  <section data-id="{{book.id}}">
    <h3>
      {{book.title}}
    </h3>
    <h6>Author:
      {{#if book.author}}
        {{book.author}}
      {{else}}
        No author provided.
      {{/if}}
    </h6>
  </section>
{{/each}}
